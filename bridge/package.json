{
  "name": "decky-bridge",
  "version": "0.1.0",
  "description": "Decky bridge server — receives Claude Code hooks, manages state, emits events via Socket.io",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "test": "vitest run --maxWorkers=1 src/__tests__",
    "test:watch": "vitest src/__tests__",
    "lint": "eslint src/"
  },
  "dependencies": {
    "express": "^4.21.0",
    "express-rate-limit": "^8.3.1",
    "socket.io": "^4.8.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "eslint": "^10.0.3",
    "socket.io-client": "^4.8.3",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.57.0",
    "vitest": "^4.0.18"
  }
}
